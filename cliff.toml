# git-cliff configuration for MC-Desktop
# https://git-cliff.org

[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

"""

body = """
{%- if version %}
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{%- else %}
## [Unreleased]
{%- endif %}
{% for group, commits in commits | group_by(attribute="group") %}

### {{ group | striptags | trim | upper_first }}
{%- for commit in commits %}
- {{ commit.message | split(pat="\n") | first | upper_first | trim }}
  {%- if commit.scope %} ({{ commit.scope }}){% endif %}
{%- endfor %}
{%- endfor %}

"""

footer = """
---
*Generated by [git-cliff](https://git-cliff.org)*
"""

trim = true

[git]
# Use conventional commits
conventional_commits = true
filter_unconventional = true
split_commits = false

# Sort commits by oldest first within each group
commit_order = "oldest"

# Parse commits into groups
commit_parsers = [
    { message = "^feat", group = "Added" },
    { message = "^add", group = "Added" },
    { message = "^fix", group = "Fixed" },
    { message = "^bug", group = "Fixed" },
    { message = "^doc", group = "Documentation" },
    { message = "^perf", group = "Performance" },
    { message = "^refactor", group = "Changed" },
    { message = "^style", group = "Styling" },
    { message = "^test", group = "Testing" },
    { message = "^chore\\(release\\)", skip = true },
    { message = "^chore\\(deps\\)", group = "Dependencies" },
    { message = "^chore", group = "Miscellaneous" },
    { message = "^ci", group = "CI/CD" },
    { message = "^build", group = "Build" },
    { message = "^revert", group = "Reverted" },
    # Catch-all for non-conventional commits
    { message = ".*", group = "Other" },
]

# Protect breaking changes
protect_breaking_commits = false

# Filter out commits
filter_commits = false

# Tag pattern to detect versions
tag_pattern = "v[0-9].*"

# Skip tags matching pattern
# skip_tags = "v0.1.0-beta.1"

# Sort tags
topo_order = false
sort_commits = "newest"
